# boutique-follow - boutique daemon for real-time updates

description	"Follows Boutique _changes to trigger real-time updates"

start on filesystem
stop on runlevel [!2345]

respawn
respawn limit 10 5
umask 022

pre-start script
    test -x /home/marcenuc/boutique/jake || { stop; exit 0; }
end script

script
  exec su -c 'cd /home/marcenuc/boutique && exec ./jake couchdb:follow 2>&1 | logger -t boutique-follow -p local0.error' marcenuc
end script
